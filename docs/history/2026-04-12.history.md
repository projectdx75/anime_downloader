# 2026-04-12

- [templates/anime_downloader_linkkf_request.html](/Volumes/WD/Users/Work/python/ff_dev_plugins/anime_downloader/templates/anime_downloader_linkkf_request.html) 에 Linkkf `request` 페이지 전용 데스크탑(1300px+) 좌우 여백(`5%`)을 추가하고, 상단 카드 레이아웃을 `포스터/기본정보`와 `액션/저장옵션` 블록으로 분리해 포스터 하단 과도한 빈 여백을 줄였다.
- `request` 에피소드 카드에 `보기` 버튼을 추가하고, 기존 로컬 재생(`play_video`)과 분리된 원본 재생(`play_remote_video`) 경로를 도입해 모달에서 원본 스트림을 바로 재생할 수 있게 했다.
- [mod_linkkf.py](/Volumes/WD/Users/Work/python/ff_dev_plugins/anime_downloader/mod_linkkf.py) 에 `resolve_remote_stream`/`proxy_remote_media` ajax 서브 라우트를 추가해 `playid -> m3u8/vtt` 해석과 HLS 프록시(playlist URI 재작성 포함)를 지원하도록 보강했다.
- 신규 `playhd2.php`(ArtPlayer) 패턴에 맞춰 `extract_video_url_from_playid()`의 m3u8/vtt 추출 정규식을 확장(`videoUrl`, `tracks[].file`)하고, 상대경로(`/r2/...`) 및 escaped URL(`\\/`) 정규화를 추가했다.
- Linkkf 상세 페이지 포스터 선택자가 변경된 이슈를 반영해 `poster_url` 추출을 `animeimglink` 계열 선택자/메타 태그 fallback까지 확장하고, `poster_url`이 비어있는 캐시는 재사용하지 않도록 `get_series_info()` 캐시 조건을 보강했다.
- 요청에 따라 `TVDB 확인` 버튼 렌더링 코드를 주석 처리해 UI에서 숨겼다.
- 플러그인 버전을 [info.yaml](/Volumes/WD/Users/Work/python/ff_dev_plugins/anime_downloader/info.yaml) 기준 `0.7.32`로 올렸다.
