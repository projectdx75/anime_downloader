# 2026-04-09

- [zendriver_daemon.py](/Volumes/WD/Users/Work/python/ff_dev_plugins/anime_downloader/lib/zendriver_daemon.py) 의 `fetch_with_browser()`를 탭 재사용 방식에서 요청당 fresh tab 확보 방식으로 변경해 `existing tab` 꼬임으로 인한 장기 지연 가능성을 낮췄다.
- 탭 확보 실패 시 즉시 `browser = None`으로 리셋해 다음 요청에서 브라우저 재기동 경로를 타도록 보강했다.
- `page.get_content()` 호출에 `asyncio.wait_for(..., timeout=3)`를 적용해 콘텐츠 수집 단계 hang으로 인한 전체 fetch 지연을 제한했다.
- navigation timeout 발생 시 polling 대기 시간을 짧게(`max_wait = 3`) 제한하고, 정상 navigation일 때만 기존보다 짧은 polling(`max_wait = 6`)을 사용해 tail latency를 줄였다.
- 플러그인 버전을 [info.yaml](/Volumes/WD/Users/Work/python/ff_dev_plugins/anime_downloader/info.yaml) 기준 `0.7.31`로 올렸다.
